# Bu proje sadece Python standart kütüphanelerini kullandığı için
# harici bir pip paketi (Flask, Django vb.) gerektirmez.
# Render.com build aşamasında hata vermemesi için bu dosya boş bırakılmıştır.
